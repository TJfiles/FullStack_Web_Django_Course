var myArr = ['one', 'two', 'three'] //create an array

var lastItem = myArr.pop() // delete or gets the last item of the array

myArr.push('New_Item') // creates one more slot in the end of the array

myArr[1] //gets index 1
myArr[myArr.length -1] //gets the last index of the array

var matrix = [[1,2,3],[4,5,6],[7,8,9]]
matrix[1][0] // output is 4, it gets the index 1 of matrix, and then index 0 of the second array inside matrix

matrix.length
console.log(myArr)


